import { SinginComponent } from "@/components/client";
import React from "react";

const signInPage = () => {
  return (
    <main>
      <section>
        <SinginComponent />
      </section>
    </main>
  );
};

export default signInPage;
